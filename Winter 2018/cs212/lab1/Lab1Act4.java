/**
   Sorts black, red, and white marbles into the order
   black, red, white.
   Solves 60-212 lab #1 activity #4 
   @author Ben Davidson 
   @version 1.0 2018-01-15
*/
int totalCount;
int marble 
for (counter = 0 ; counter <= totalCount ; counter++)
    {
        for (innerCount = 0 ; innerCount <= totalCount ; innerCount++)
        {
            if marbleColour is red
                increment position in sort 
             if marbleColour is white
                increment position in sort
        }

