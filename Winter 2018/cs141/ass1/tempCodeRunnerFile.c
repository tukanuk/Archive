 // int magicCounter = 0;
    // int currentNumber = 0;

    // scanf("%d", &magicCounter);
    
    // for (int i = 0 ; i < magicCounter ; i++)
    // {
    //     scanf("%d", &currentNumber);
    //     printf("%d ", generateMagicNumber(currentNumber, reverseInteger(currentNumber)));
    // }
    //     printf("\n%d\n", largestDiff);